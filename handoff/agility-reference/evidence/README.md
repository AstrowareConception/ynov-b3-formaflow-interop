# Preuves sélectionnées

Le paquet référence les résultats sans dupliquer tout le dépôt : contrat et exemples, matrice writer/reader, test d'ancien client, scénario poison/DLQ, benchmark contextualisé et démonstration d'idempotence. Dans le dépôt source, exécuter `python scripts/tasks.py quality`; dans cette copie autonome, exécuter uniquement le validateur local.

